class Posts{

  String address;
  String contact;
  String email;
  String firstName;
  String lastName;
  String password;

  Posts(this.email, this.password, this.address, this.contact, this.firstName, this.lastName);
}